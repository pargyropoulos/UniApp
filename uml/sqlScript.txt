CREATE TABLE department (
  id        integer GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), 
  name      varchar(255), 
  school_id integer NOT NULL, 
  PRIMARY KEY (id));
CREATE TABLE school (
  id             integer GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), 
  universityname varchar(255) NOT NULL, 
  name           varchar(255) NOT NULL, 
  PRIMARY KEY (id));
CREATE TABLE university (
  name        varchar(255) NOT NULL, 
  description varchar(255), 
  info        varchar(255), 
  counter     integer, 
  PRIMARY KEY (name));
ALTER TABLE department ADD CONSTRAINT FKdepartment668978 FOREIGN KEY (school_id) REFERENCES school (id);
ALTER TABLE school ADD CONSTRAINT FKschool705739 FOREIGN KEY (universityname) REFERENCES university (name) ON DELETE Cascade;
